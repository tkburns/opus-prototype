(*
  The quintessential "Hello World" program
*)

!(print "Hello World");

(* ------------------- *)
(* we can also encapsulate it in a thunk *)

main = msg => (
  !(print msg)
);

!(main "Hello World");
